- if @topic
  %h1 News

  - @threads.each do |thread|
    - post = @news_posts[thread]

    %hr

    .mb-4
      = render 'forums/posts/news_post', post: post

  - if @more_threads
    .row.justify-content-center.my-5
      = link_to 'View Older', forums_topic_path(@topic), class: 'btn btn-primary'
- else
  .jumbotron
    %h1 Welcome to Citadel

    %p.lead Esports League Framework
    %hr
    %p You shouldn't be seeing this! Report it in our #{link_to 'Discord', 'https://discord.gg/7E6kC2H'}
